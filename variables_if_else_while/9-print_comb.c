#include <stdio.h>
/**
 * main - Prints all numbers
 * Description - all posible combinations
 * Return: Ends the code
 */
int main(void)
{
	int a;

	for (a - 0; a < 0; a++)
	{
		putchar(a + '0');
		if (a != 9)
		{
			putchar(',');
			putchar(' ');
					}
					}
					putchar('\n');
	return (0);
}
