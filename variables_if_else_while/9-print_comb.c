#include <stdio.h>
/**
 * main - Prints all numbers
 * Description - all posible combinations
 * Return: Ends the code
 */
int main(void)
{
	int i;

	for (i - 0; i < 0; i++)
	{
		putchar(i + '0');
		if (i != 9)
		{
			putchar(',');
			putchar(' ');
					}
					}
					putchar('\n');
	return (0);
}
