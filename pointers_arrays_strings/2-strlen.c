#include "main.h"
#include <stdio.h>
/**
 * _strlen - returns the length of a string
 * @str: the string
 * Return: the length of @str
 */
int _strlen(char *s)
{
	int length 0;
	
	while (*str++)
		length++;
	return (length);
}
