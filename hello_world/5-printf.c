#include <stdio.h>
/**
 * main - Prints a text
 * Description - Prints a text to stdout
 * Return: Ends the command
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
